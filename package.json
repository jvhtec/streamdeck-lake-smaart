{
  "name": "com.jvhtec.lake-smaart",
  "version": "1.0.0",
  "description": "Stream Deck plugin for Lake LM and Smaart",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('com.jvhtec.lake-smaart.sdPlugin/dist',{recursive:true,force:true})\"",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "watch": "tsc -w",
    "check": "npm run typecheck && npm run build",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.5.0"
  }
}
